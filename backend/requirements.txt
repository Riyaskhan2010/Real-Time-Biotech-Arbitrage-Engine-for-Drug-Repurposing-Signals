# Core framework — pinned to tested versions
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database
sqlalchemy==2.0.30
alembic==1.13.1

# Pydantic — pinned together (core must match)
pydantic==2.7.1
pydantic-core==2.18.4
pydantic-settings==2.2.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3

# Utilities
python-multipart==0.0.9
python-dotenv==1.0.1
httpx==0.27.0

# OpenAI — pin to v1.x (v3 is a different/older package)
openai==1.30.1

# Async file handling
aiofiles==23.2.1

# Email validation (needed by fastapi)
email-validator==2.1.1
